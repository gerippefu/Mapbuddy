import { Router } from 'express';
const router = Router();

// TODO: districts CRUD placeholder
router.get('/', (_req, res) => {
  res.json({ todo: 'Districts API to be implemented' });
});

export default router;